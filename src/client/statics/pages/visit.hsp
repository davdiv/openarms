# template template(tab)
    <div class="container-fluid">
        Visite
    </div>
# /template

# template tabTemplate(tab)
    <span class="glyphicon glyphicon-calendar"></span> {tab.title}
# /template

module.exports = {
    template : template,
    tabTemplate : tabTemplate
};
