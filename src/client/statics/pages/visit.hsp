var personField = require("../components/person/personField.hsp.js");

# template template(page)
    <div class="container-fluid">
        Visite<br>
        <#personField value="{page.personId}" />
    </div>
# /template

# template iconTemplate(page)
    <span class="glyphicon glyphicon-calendar"></span> {page.title}
# /template

module.exports = {
    template : template,
    iconTemplate : iconTemplate,
    init: function(page){
        page.personId = page.query.person;
    }
};
