var personRef = require("../personId/personRef.hsp.js");

# template viewFamily(data)
    {if data.people}
        {foreach person in data.people}
            <#personRef data="{person.id}"/>{if !person_islast}, {/if}
        {/foreach}
    {/if}
# /template

module.exports = viewFamily;