var navBar = require("./components/navBar.hsp.js");

# template main(tabMgr)
    {let activeTab = tabMgr.activeTab}
    <#navBar tabMgr="{tabMgr}"/>
    {if activeTab && !activeTab.processing}
        <#activeTab.template tab="{activeTab}" />
    {/if}
# /template

module.exports = main;